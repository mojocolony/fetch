const DEFAULT_ENDPOINT='https://appesztafatypbxzdunr.supabase.co/functions/v1/fetch-capture';const endpoint=document.getElementById('endpoint'),token=document.getElementById('token'),saved=document.getElementById('saved');chrome.storage.sync.get(['endpoint','captureToken']).then(v=>{endpoint.value=v.endpoint||DEFAULT_ENDPOINT;token.value=v.captureToken||''});document.getElementById('save').addEventListener('click',async()=>{await chrome.storage.sync.set({endpoint:endpoint.value.trim()||DEFAULT_ENDPOINT,captureToken:token.value.trim()});saved.textContent='Saved';setTimeout(()=>saved.textContent='',1500)});
